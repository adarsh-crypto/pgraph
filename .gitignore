# pgraph's own dev artifacts
.venv/
__pycache__/
*.egg-info/
.pytest_cache/
dist/
build/
*.py[cod]
.DS_Store

# Personal, machine-local Claude Code overrides
.claude/settings.local.json

# A project's live graph DB is machine-local; the JSONL export is what you commit.
.pgraph/graph/
.pgraph/graph*
